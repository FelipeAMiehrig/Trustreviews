from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='keyword extraction an topic matching for user reviews',
    author='Felipe Miehrig',
    license='MIT',
)
